# ?�無?��??�》�??��?覆核?��? ??Grok R4（td-r54-v1 ?�度層�?

| ?�目 | ?�容 |
|------|------|
| ?�件�?? | `GROK_REVIEW_td_R4` |
| 範�? | R53 ?�度層�??�口?��?死亡?�散／�?�??字�?波次?��?／Boss slow-mo／無�?streak／女神�??��?塔�?級�??��?WebAudio／`effectSeed` |
| 對照�?�� | ?��??��?�?RNG ?�離（�???實波不破）�?Boss slow-mo ?�「爽?�」�?高波�?thrash ?�控；WebAudio ?�解?�可?�用；`reduced` 完整?��? |
| 審查??| Grok（監工�?對�??��?角�? |
| ?��? | 2026-07-11 |
| ?��? | **?�審不改**；�? `src/game.js`?�`src/ui.js`?�`src/rules.js`?�`index.html`?�`scripts/test-td-e2e.js`；�?論�?**檔�?:行�?**?��?小�???|

**結�?標籤**

| 標籤 | ?�義 |
|------|------|
| **PASS** | 契�??�地，主路�??��?�?|
| **BUG** | ?��??��??�輯／�?約錯�?|
| **RISK** | ?��??��?但�?�?��?�副作用?��???thrash?��? a11y ?��?�?�� |
| **NOTE** | 設�??�捨?�測試�??�缺??���?件用語差??|

**?��?�?*

| 等�? | ?�義 |
|------|------|
| **P0** | ?��?決�??�主契�?／�??��??��??�寫?�戰鬥�??�軸 |
| **P1** | 高波次�??�、音訊�?點、reduced �?��?�實作落�?|
| **P2** | ?��??�命?��?UI ?��??��?風險 debt |

---

## 0. 總�?（�?講人話�?

| 檢查??| 結�? | 等�? |
|--------|------|------|
| (1) `effectSeed` ?��?�?RNG ?�離；�???實波不破 | **PASS** | ??|
| (2) Boss slow-mo ??render ?�是 game step | **BUG／RISK：�? `update(dt)` ?�輯�?* | P0 |
| (3) ?�幣飛�?／�???高波次洩漏�?節�?| **RISK：�? DOM 洩�?，�??�硬上�?** | P1 |
| (4) WebAudio �???��?點�???| **RISK：解?�可樂�?誤�?；�?點�??�用** | P1 |
| (5) reduced 完整?��? | **RISK：�??��??��??�新增強?��??��??��??��?** | P1 |

**一?�話**：`effectSeed` ?�波次決定性主契�?**?�離�?��**，�???實波不�?被�?子隨機�??��?�?Boss slow-mo **不是純畫??*?�—�?縮放?�段 `update` ??`dt`，�??�慢?�怪、�??�、冷?�、`state.clock` ?��??��?續�??�。�?子走 **canvas ???** ?��? DOM，�??��?點洩漏�???*缺全?�硬上�?**；WebAudio **每次?�建 Oscillator/Gain**；`reduced` ?��???R53?�新增強?��??��??��?**?�害浮�?／`burst`／`ring`／�??��??��?**??
**e2e 守�??��?**（`scripts/test-td-e2e.js:1098-1101`）�??��??��??�口／�?�???�散／�??��?streak／slow-mo?��??�reduced ?�這幾?��??�」�?**不�?** slow-mo ?�否?��?輯、�?子�??�、Audio 節點�??�、`burst`/`flashText` ??reduced 下是?�歸?��?
**?�輪?�改任�?程�???*

---

## 1. effectSeed 粒�??��? vs ?�輯 RNG（�???實波�?
### 1.1 證�???
| 檢查 | 結�? | 證�? |
|------|------|------|
| ?��? RNG ?��??�?��?�?| PASS | `state.effectSeed` ?��?寫入：`src/game.js:376`；推?��?`effectRand` `src/game.js:168-177` |
| 種�?來�?不進波�?LCG | PASS | `effectSeed = (runSeed ^ (affixSeed<<1) ^ 0x9e3779b9)`（`:376`）�???`waveRngSeed` 混種式�???|
| 波次／�??�用 `waveRngSeed` | PASS | `waveSeedFor` ??`TDRules.waveRngSeed`：`src/game.js:425-435`；`previewNextWave`／`wavePlanFor`：`437-455` |
| `rules.js` 決�??�波�?| PASS | `waveRngSeed`：`src/rules.js:399-402`；`makeRng`：`415-416`；`rules.js` ?�無 `Math.random`（測�?`scripts/test-rules.js:65`�?|
| `effectRand` ?�叫點�??��? | PASS | ??`burst`（`game.js:1427-1429`）�? `flashText`（`:1477`�?|
| 粒�??�新不�?寫戰鬥�???| PASS | `update` 粒�?段只??`p.life/x/y` ??filter：`src/game.js:912-925` |

### 1.2 ?�離結�?

- **?��?=實波**：`previewNextWave` ??`startWave` ?�用 `waveSeedFor(w)` ???��? `runSeed:affixSeed:wave` ?��?`game.js:425-435, 439-445`）。`effectSeed` **不在**?��?路�?上�?- **粒�??��?不�??��? queue**：即便戰鬥中大�? `burst`／`flashText` ?��?`effectSeed`，�?一�?`waveSeeds[key]` 仍只?�決??run／affix／wave??- **e2e 已�???seed ?��?=實波守�?**（歷??R 輪�?`test-td-e2e.js` �?`1428-1443` ?�段�?，本輪�?確�???RNG 源�? `effectRand` ?��???
### 1.3 ?��? NOTE（�? effectSeed ?��?約�?�?determinism ?�支�?
| ?�目 | 標籤 | 證�? | 說�? |
|------|------|------|------|
| ?�人 `animSeed` / `_dodgeRoll` ??`Math.random()` | NOTE／既??RISK | `game.js:579-581` | ?�避??*?�輯**且�?決�??��???`effectSeed` ?��?，�?不碰 wave seed |
| ?��? grass／decor ??`Math.random()` | NOTE | `game.js:406-418` | 純�?覺�??��?不進波�?|
| `performanceLow()` ?��? `burst` 次數 | NOTE | `game.js:1424-1426` | ?�改�?`effectRand` 消耗�? ??**?��?序�?**跨�?置�??��??��?仍�??��?�?RNG |

**?��?總�?：PASS（�??��???實波不破?��?約�?立�???*

**?�小�??��??�離�?*

1. `newGame({ runSeed: 111111, affixSeed: 777 })`，�???`previewNextWave().queue`??2. ?��? `burst`／�?殺刷粒�??�百次�?確�? `state.effectSeed` 已�???3. ??`previewNextWave()`／`startWave` ?��???queue ?�步�?1 ?��?；`waveSeedFor` 不�???
---

## 2. Boss slow-mo：�? render ?�是 game step�?
### 2.1 觸發?��???
| 步�? | 證�? |
|------|------|
| Boss ?�殺寫入 | `killEnemy`：`state.slowMoLeft = reduced ? 0 : 0.2`?�`slowMoScale = 0.35`（`game.js:1167-1170`�?|
| 主迴??| `loop`：`update(dt)` ??`render()`（`game.js:787-788`�?|
| slow-mo 套用�?| **`update` ?�頭**對傳??`dt` ??`dt *= scale`（`game.js:808-816`�?|

```808:816:src/game.js
  function update(dt) {
    const rawDt = dt;
    if (state.slowMoLeft > 0 && !reducedEffectsEnabled()) {
      const scale = Math.max(0.15, Math.min(1, state.slowMoScale || 0.35));
      state.slowMoLeft = Math.max(0, state.slowMoLeft - rawDt);
      dt *= scale;
    } else if (state.slowMoLeft > 0) {
      state.slowMoLeft = 0;
    }
```

### 2.2 被縮?��??�輯（�?�?render�?
縮放後�? `dt` 餵給?�段 `update`，�??��?不�??��?

| 系統 | 行�?（�?�?| 影響 |
|------|------------|------|
| ?��?timer | `818-823` | ?��??��?變慢 |
| ?�??CD | `827-829` | ?�卻變慢 |
| combo ?��? | `832-835` | ??��窗�??��??�戲?��?�?|
| 女�? smite CD | `844` | ?��?變慢 |
| ?�移?��?�?CD／�?�?| 後�? `update` ?��? | ?��??��??�脹 |
| `state.clock += dt` | `927` | **?�?��? clock ??until ?�??*（slow／freeze／mute／vuln?��??��?延長 |
| 粒�? life | `913-914` | ?��?也�??�慢（�??��??�「只?�慢?��? |

**例�?（刻?�用 rawDt�?*

| ?�目 | 行�? | 行為 |
|------|------|------|
| `slowMoLeft` ?�數 | `812` | ??`rawDt` ???��?�?0.2s 結�?（在 `state.speed===1` ?��? |
| `redVignette` 衰�? | `842` | ??`rawDt`，�?�?slow-mo |

### 2.3 render ??
- `render()`（`1512-1529`�?*沒�?**?��? time-scale；只?�當??state??- 結�?�?*?��?作是?�輯步縮??*，畫?��??�是?��??��?不是?�只??render?��?
### 2.4 對�??�影?��??��?�?P0�?
| ?��? | 說�? |
|------|------|
| 平衡／�?�?| Boss 死�? 0.2s ?��??0.35）出?�、�??�、�???CD ?�慢 ???�暫?��?費思考�?輸出窗」�?�?*?��?增�?**不只 juice |
| 決�??��???| ?�未來�? deterministic replay，特?��??��?reduced 清�? slow-mo：`1169`?�`814-815`）�?�?*?��??��?序�???clock 軌跡?�歧** |
| ??`state.speed` ?��? | 主迴?��? `dt *= state.speed`（`787`）�???`update`；slow-mo ??`rawDt` 已含?��???2? �?slow-mo ?��??�短 |

**?��?總�?：BUG／RISK（P0）�?�?��?�為?��??�度／�??�面?��?實�???game step time-scale??*

**?�小�???*

1. `setReducedEffects(false)`，�???boss?�`hp=1`，�?殺�?2. 確�? `slowMoLeft > 0`（e2e 已�?：`test-td-e2e.js:1007-1008`）�?3. ?��??��?比�?：放一??`speed` 已知?�怪�?`step(0.05)` 位移 ??`speed * 0.05 * 0.35`，而�? `speed * 0.05`??4. `setReducedEffects(true)` ?�殺 boss ??`slowMoLeft === 0`（`1169`）�?位移?�復�?��??
---

## 3. ?�幣飛�?／�????DOM／canvas 洩�??��?�?
### 3.1 實�?形�?

| ?��? | ?��? | 建�? | ?�收 |
|------|------|------|------|
| ?�幣飛�? | `state.particles` ?�件（`text` + `toX/toY`�?| `coinFloat` `1439-1444` | `life` ??? filter `925` |
| 死亡?�散 | ?�陣?��?`burst` + `ring`�?| `deathBurst` `1432-1437`；`killEnemy` ?��? `burst` `1163` | ?��? |
| ?�口／�??��??�害�?| ?�陣??| `muzzleFlash`／`upgradeBeam`／`flashText` | ?��? |
| ?��??��? | **DOM class** `canvas.shake` | `screenShake` `1468-1472` + CSS `index.html:69-70` | `setTimeout` 300ms remove |

**沒�?**?��??��?�??粒�?建�? DOM 節點�?飛�???**canvas `fillText`**（`drawParticle` `2122-2130`）�?
### 3.2 洩�??��?

| ?�慮 | ?��? | 說�? |
|------|------|------|
| DOM 節點累�?| **?��?PASS�?* | ?��?不�? HTML 子�?�?|
| `particles` ???永�?不縮 | **?��?構性洩�?* | 每�? `filter(life > 0)`（`925`�?|
| `screenShake` timeout ?��? | **�?RISK** | 多次 `add("shake")` + 多�?timeout；�?增�?點�?但可?��?�?CSS ?�畫 |
| ????�硬上�? thrash | **RISK P1** | 見�? |

### 3.3 節流現況�?對照表�?

| API | ?��??�制 | reduced | 高波次風??|
|-----|----------|---------|------------|
| `burst` | `n>14` ??reduced ?�到 8；`performanceLow` ?0.45（`1424-1426`�?| **減�??��???* | 每殺仍可?��?�?|
| `deathBurst` | boss 72 + ring ?�環（`1435-1437`�?| ?�段 return（`1433`�?| �?��模�?尖刺 |
| `coinFloat` | **每殺 1 條�???cap**（`1439-1444`�?| return | 群死??N 條並�?|
| `muzzleFlash` | 每發 1，無 cap（`1446-1451`�?| return | ?��?�?? 多�? |
| `flashText`／`damageNumber` | **??cap?�無 reduced**（`1475-1486`�?| 仍�???| ?�害字海 |
| `ring` | **??* `performanceLow && particles.length > 24` ??skip（`1464`�?| **不檢??reduced** | �?��?�質幾�?不�?�?|
| ?��? `MAX_PARTICLES` | **不�???* | ??| update/render ??O(n) ?��?（`913-925`, `1522`�?|

### 3.4 高波次�??�估算�?對�?�?
?�次?�通�?殺�???reduced）�?

- `burst(..., 12)` ??12  
- `deathBurst` ??`burst(..., 22)` + `ring` ??23  
- `coinFloat` ??1  
- ?��? **??36** 粒�?／殺（�??��??�中 `burst(5)`?�傷害浮字、combo 字�?

?�砲?�中（`1270-1274`）�?`burst 12` + `burst 28` + `ring` ??**41**／發??
群死 30 ?��?秒�?窗口?�瞬??**1000+** ????��?；�?幀??`filter` ?�陣?�。弱裝置上�???`performanceLow` ??0.45 ?��?賽�?，�? **quality 仍為 high ?�幾乎無?��?**??
**?��?總�?：RISK（P1）�???DOM 洩�?；�? canvas 粒�? thrash／缺硬�??��?*

**?�小�???*

1. �?��?��?，場上�?�?+ 大�?低�??��?幀清場?? 
2. ??`update` 後�? `state.particles.length` 峰值�? 
3. ?��?：可?��? 24（`ring` ?�值只??low ?�質?��?）�??��?後�? life ?��??�落，�?峰值�??�本高�?
---

## 4. WebAudio：解?��?節點�???
### 4.1 �??路�?

| 步�? | 證�? | 評註 |
|------|------|------|
| ?��?一次性�???| `pointerdown`／`keydown`／`touchstart`，`{ once: true }`（`game.js:133-135`�?| 標�??��? |
| `unlockAudio` | �?`AudioContext`?�`resume()`??*立刻** `unlocked = true`（`122-131`�?| ?��? `resume` Promise?�未確�? `state === "running"` |
| ?�放?��?�?| `playSfx` ?�呼??`unlockAudio()`（`136-140`�?| ?��??�勢外路�?|
| ?�音?�路 | `audioMuted()` ??unlock ?�接 return（`123`）�?`playSfx` �?return（`137`�?| 見�??��??��???RISK |
| UI ?�??| `getJuiceSettings().audioUnlocked`（`118-119`）�?`ui.js:1241-1248` | 顯示?�已�??／�?次�?作�??�用??|

### 4.2 節點�??�週�?

```151:166:src/game.js
    try {
      const now = ac.currentTime;
      const osc = ac.createOscillator();
      const gain = ac.createGain();
      // ... frequency / gain ramps ...
      osc.connect(gain);
      gain.connect(ac.destination);
      osc.start(now);
      osc.stop(now + spec[1] + 0.03);
    } catch {}
```

| 檢查 | 結�? | 說�? |
|------|------|------|
| Oscillator／Gain 池�??�用 | **??* | 每次 `playSfx` `createOscillator` + `createGain` |
| ?�放�?`disconnect` | **??* | 依賴 `stop` �?GC |
| ??kind ?�用節�?| **??* | fire／hit 高頻?�短?��?大�?建�?�?|
| `AudioContext` ?��? | **??* | `audioState.ctx` ?�用（`127`�?|

### 4.3 RISK ?�細

| # | 標籤 | 等�? | ?��? | 證�? |
|---|------|------|------|------|
| A1 | RISK | P1 | **樂�? `unlocked = true`**：`resume()` ?��?步失?��?�?`suspended` ?��?UI 顯示已解?��?不�??�試（�? `unlocked` ?�退�?| `122-131`, `123` |
| A2 | RISK | P1 | **?�音?�第一次�???*：`unlockAudio` early return，�? listener �?`once` 消耗�?之�? unmute ?��? `playSfx?�unlockAudio`，在?�格?�勢?��??�覽?�可?��??�失??| `123`, `133-135` |
| A3 | RISK | P1 | **?��?點�??��?節�?*：速�? `fire` 每發 `playSfx("fire")`（`1253`）�??�中 `hit`（`1298`）�?群殺 `kill`（`1166`）�? Oscillator 尖峰 | `151-165` |
| A4 | NOTE | P2 | reduced **不�???*；慶祝�? `playSfx("wave")`（`506`）�???return | 設�??�接?��???bug |

**?��?總�?：RISK（P1）�?Context ?�單例�?�???��??�鉤子�?不穩?��?節點零?�用??*

**?�小�???*

1. ?��?不�??�面，用 debug `playSfx("fire")`（若?��??�許）�? ?�能?�聲�?`audioUnlocked` 仍被�?true?? 
2. ?�勾?�音?�第一次�??��??��??�音後�??????��?行�??�覽?�無?��? 
3. 多座高�??��?，Performance／WebAudio ?�板觀�?Oscillator 建�??��???
---

## 5. reduced?��??��??�」�?

### 5.1 ?��??��???
| 層�? | 證�? | 語�? |
|------|------|------|
| UI ?��? | `index.html:837`??*減�?**?��??��?`ui.js:1248`?�特??**減�?**／�??��?| ?��?語�???**reduce**，�???hard off |
| 讀??| `reducedEffectsEnabled`：`localStorage td_reduced_effects` **??* `prefers-reduced-motion`（`game.js:90-96`�?| a11y OR |
| 寫入 | `setReducedEffects` ?�步 flash ?��??�兩??localStorage key（`98-105`�?| ?��? flash 一併�? |

### 5.2 ?��??�陣（�??�「�??�」宣稱�?

| ?��? | reduced 行為 | 行�? | 完整?��?�?|
|------|--------------|------|------------|
| ?�口??`muzzleFlash` | return | `1447` | ??|
| 死亡?�散 `deathBurst` | return | `1433` | ??|
| ?�幣飛�? `coinFloat` | return | `1440` | ??|
| ?��??�柱 `upgradeBeam` | return | `1454` | ??|
| 波次?��?橫�?／�? | `celebrateWaveClear` ?��? return�?*仍播 sfx**�?| `505-507` | ?��? ?��? |
| Boss slow-mo | 不寫?��??��???| `1169`, `810-815` | ??|
| ?��??��? | return | `1470` | ??|
| `burst`（命中�??�殺外層／�??��? | ??`n>14` ??8�?*�?burst ?��?** | `1424` | ??|
| `ring` | **??reduced 檢查** | `1463-1465` | ??|
| `flashText`／傷害�?／�??��?�?| **??reduced 檢查** | `1475-1479` | ??|
| 女�??��?紅�? `redVignette` | 仍設 **0.24**（�???0.55�?| `980` | ??減�? |
| ??hitFlash | �?`reducedFlashEnabled` | `646-647` | ?��???effects ???寫入�?|
| Combo／NO LEAK HUD | 仍繪�?| `1570-1604` | ?��??�輯 HUD，�?粒�?�?|
| WebAudio | 不�? | `136+` | ?��??��??�音�?|

e2e ?��?定「新增強?��??��??��?`test-td-e2e.js:1010-1024, 1100-1101`）�?

- reduced 下�???muzzle／coin／beam／slow-mo ??**?��?式�???*  
- **??*?��? `particles.length === 0` ?�無 `burst`／傷害�?  

### 5.3 結�?

- ?��?約是 **?�R53 ?��?強特?�在 reduced ?��???* ??**PASS**（�? e2e 一?��??? 
- ?��?約是 **?�reduced = 完整?��?一??juice??* ??**FAIL／RISK P1**（`burst`／`ring`／`flashText`／�??��??��?仍在）�? 
- UI ?�己寫「�?少�?減�??��??�「�??��??�」�???*不�???* ???�件／�??�用語�?對�???
**?��?總�?：RISK（P1）相對「�??��??�」�?PASS ?��??�R53 子�? guard?��?*

**?�小�???*

1. `setReducedEffects(true)`，`fireTower` 殺怪�? 
2. ?��?：無 `muzzle`／`toX` ?�幣／`beam`�?*仍可??*??`burst` 粒�??�傷�?`text`（`hit`?�`burst(5)` `1286` + `damageNumber` `1299`）�? 
3. `leak` 一????`redVignette ??0.24`（`980`）�?
---

## 6. ?��??�查（監工用�?
### (1) effectSeed ?�離

| 結�? | **PASS** |
|------|----------|
| ?�鍵�?| `effectRand` `game.js:168-177`；seed ?��?`:376`；波�?seed `:425-435`；`rules.js:399-402` |
| ?��?=實波 | 不�?�?`effectSeed`；�?子無法改 queue |

### (2) Boss slow-mo

| 結�? | **??game step（`update` ??`dt *= scale`）�??�只 render** |
|------|----------|
| ?�鍵�?| 觸發 `1167-1170`；�???`808-816`；clock `927`；render ??scale `1512-1529` |
| 標籤 | **BUG／RISK P0**（若規格要�?純表?�層�?|

### (3) 飛�?／�???thrash

| 結�? | **??DOM 洩�?；canvas ?????life ?�收?�無硬�???* |
|------|----------|
| ?�鍵�?| `coinFloat 1439-1444`；`deathBurst 1432-1437`；`burst 1423-1430`；filter `925`；ring 軟�? `1464` |
| 標籤 | **RISK P1** |

### (4) WebAudio

| 結�? | **?��??�解??+ Context ?��?；無節點�??��?unlocked 樂�?** |
|------|----------|
| ?�鍵�?| `121-166`；觸??`1253, 1298, 1166, 506, 982` |
| 標籤 | **RISK P1** |

### (5) reduced

| 結�? | **R53 強特?��??��??��??�全 juice ?��?** |
|------|----------|
| ?�鍵�?| ?��? `90-105`；�???`1433-1470, 507, 810, 1169`；�?�?`1424, 1463-1465, 1475-1479, 980` |
| 標籤 | **RISK P1**（�??��??��??�」�?；e2e 子�? **PASS** |

---

## 7. 建議修復?��?序�??�建議�?實�?�?
| ?��? | 建議 | 對�? |
|------|------|------|
| P0 | slow-mo ?�為 **純表??*：�?�?render ?�值�??��? `fxTimeScale`�?*不�?**乘進出?��?CD／`state.clock`；�??�件?�確寫「Boss ?�殺?�暫?��??��??�脹?�並納入平衡 | §2 |
| P1 | `state.particles` **硬�???*（�???drop ?�?��??�併飛�?）�?`flashText`／`coinFloat` ?��?�?cap | §3 |
| P1 | `playSfx`：�?�?+ ?�選輕�? pool；`unlocked` ?�在 `running` 後置 true；�??��?不�? `once` ?��??�勢 | §4 |
| P1 | ?��??��??��??�」�?`burst`／`ring`／`flashText`／�??��?�?gate；�???UI／�??�改?�「�??�」避?��??�歧�?| §5 |
| P2 | e2e 補�?slow-mo 下�?移倍�?；reduced �?`particles` ?��??��???seed 下�??�特??queue 不�?（�?歸�?�?| §0 |

---

## 8. 簽署

| ?�目 | ?�容 |
|------|------|
| 審查模�? | ?�審不改 |
| 主�?檔�? | `src/game.js`（特?��?slow-mo／audio／particles）、`src/ui.js`（juice UI）、`src/rules.js`（wave RNG）、`index.html`（shake CSS／toggle）、`scripts/test-td-e2e.js`（R53 子�?守�?�?|
| ?�出 | `docs/GROK_REVIEW_td_R4.md` |
| 下�?步�?給實作者�? | ?�決 P0 slow-mo 規格；�?修�?子硬上�???reduced 語�?對�? |

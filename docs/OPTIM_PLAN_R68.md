# 無盡塔防 R68 地圖完整顯示檢修計畫

輪次代號：td R68
日期：2026-07-16
依據：`C:\Users\digimkt\.claude\skills\game-optimization-round\SKILL.md`、`docs/AUDIT_full.md`、`AGENTS.md`

## P0 與驗收基線

- 實測視口：1366×600 非觸控桌機。
- 修正前 canvas：`266.2,104.9 → 852.2,495.9`，尺寸 `586×391`。
- 修正前 dock：`69.2,354 → 1049.2,486`，與 canvas 交疊 `586×132`，導致戰場下緣不可見、不可點。
- 目標：960×640 內部棋盤在所有指定視口等比完整顯示；canvas 四角與中心 hit-test 都命中 canvas；dock 與 canvas 交疊面積為 0；所有核心控制仍在視口內且可點。

## 八大面向對照與本輪驗收

1. 美術
   - 本輪不重畫 R65 地圖、塔或角色素材，維持既有主題與像素色盤。
   - 驗收：等比縮放不拉伸、不裁切，canvas CSS 比例維持 3:2；三視口證據可看見整張地圖。

2. 按鈕
   - 將建塔 dock、技能盤、開始/速度/暫停/設定改為保留空間的版面列，不再 absolute/fixed 疊在地圖上。
   - 1366×600、1280×640 等矮寬桌機與 844×390 橫向手機採側置控制欄，直式手機保留底部控制列。
   - 驗收：R66 控制守門全綠；10 建塔、5 技能與核心動作鈕 ≥44px、中心可命中、互不重疊、無頁捲。

3. 選單
   - 保持既有情報/英雄/工具抽屜與 overlay 可關閉流程，不新增長捲動或死路。
   - 驗收：RWD main/settings 矩陣全綠；難度與地圖選項仍完整可見可點。

4. 人物
   - 本輪不變更英雄或敵人視覺動畫，不以整張圖位移/縮放冒充動作。
   - 驗收：既有英雄、敵人動畫單元與 E2E 攻擊時點守門全綠。

5. 地圖模型
   - 以戰場可用容器為唯一縮放基準：可用高度已扣除頂部 HUD 與控制 dock；canvas 取寬高縮放較小值。
   - 驗收：1920×1080、1440×780、1366×600、1280×640、390×844、844×390 六視口 canvas 完整在視口與戰場容器內，無內捲、無裁切、無 dock 交疊。

6. 技能
   - 技能盤保留在畫面內常駐，R67 有效命中才消耗與觸控取消邏輯不變。
   - 驗收：5 個技能鈕與取消入口保持可達；R67 E2E 回歸全綠。

7. 腳色樣子
   - 本輪不新增或替換角色素材，不宣稱新增角色美術成果。
   - 驗收：既有角色 atlas 與亮度/飽和度成果不受 CSS 版面改動影響。

8. 動作流暢度
   - physics root、collider、英雄 attack anticipation/impact/recovery、敵人 hurt/death 管線均不修改。
   - 驗收：`npm test` 的 R62/R63 動畫守門與 `npm run test:e2e` 的命中幀回歸全綠。

## 固定品質閘門

- `npm test`
- `npm run test:e2e`（包含 R66 控制守門、R68 六視口地圖完整可見守門與既有遊戲 E2E）
- `npm run test:rwd`
- 效能出貨閘：桌機/手機 p95 ≤18ms，三跑取中位
- 版本 bump：`0.6.8` / `td-r68-v1`，舊執行版號 grep 歸零
- 秘密掃描排除 `.git`、`node_modules` 後零命中
- before/after 量測與三視口截圖進 `docs/evidence/R68/`
- 產出 `docs/CODEX_RESPONSE_td_R68.md`
- 本地繁中 commit，不 push
